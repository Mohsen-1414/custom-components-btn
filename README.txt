npm package library 

In this package, only a button component was implemented with all the testing with Jest and implementation of Storybook. 

To install, need to run <npm i custom-component-lib-btn>. 